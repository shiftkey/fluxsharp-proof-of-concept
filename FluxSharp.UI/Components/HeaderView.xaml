<UserControl x:Class="FluxSharp.Components.HeaderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="To-Dos" FontSize="24" HorizontalAlignment="Center" />
        <CheckBox x:Name="allChecked" HorizontalAlignment="Right" Margin="0,0,6,0" VerticalAlignment="Center" />
        <TextBox Grid.Row="1" x:Name="newToDo" />
    </Grid>
</UserControl>
